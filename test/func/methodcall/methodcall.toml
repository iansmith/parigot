## Note that because this is consumed from the parigot root dir, the paths provided
## are relative to that dir, not this dir

# the names here have no significance, they are just for humans

server = [
    { Name="foo", Path="build/methodcallfoo.p.wasm"},
    { Name="bar", Path="build/methodcallbar.p.wasm"},
    { Name="log", Path="build/log.p.wasm"},
]

test = [
    #{Name="methodcall_test", Path= "build/methodcalltest.p.wasm"},
]

## flags
testmode = true
remote = false
