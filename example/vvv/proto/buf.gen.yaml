version: v1
plugins:
  - name: go
    out: gen
    opt: paths=source_relative
  - name: connect-go
    out: gen
    opt: paths=source_relative
    ## you only need this plugin if you want to generate a grpc client
    ## but it worth noting that without it, you will get *only* Messages
    ## from your .proto files, not the Services. connect generates the
    ## servics from the start, and that's our default.
  #- name: go-grpc
  #  out: gen
  #  opt: paths=source_relative
  - name: parigot
    out: gen
    opt: paths=source_relative

