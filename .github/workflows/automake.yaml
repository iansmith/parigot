name: Tests and Build
'on':
  push:
    branches:
      - '*'
    tags:
      - v*
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Build
        run: make
        #
